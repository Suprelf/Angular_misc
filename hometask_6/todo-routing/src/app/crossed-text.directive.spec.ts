import { CrossedTextDirective } from './crossed-text.directive';

describe('CrossedTextDirective', () => {
  it('should create an instance', () => {
    const directive = new CrossedTextDirective();
    expect(directive).toBeTruthy();
  });
});
